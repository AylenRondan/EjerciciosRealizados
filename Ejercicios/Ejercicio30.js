
//30. Write a JavaScript program to check whether a string "Script" presents at 5th (index 4) position in a 
//given string, if "Script" presents in the string return the string without "Script" otherwise return the 
//original one.

const prompt = require("prompt-sync")({ sigint: true });
const str = prompt("Ingrese una palabra: ");

arraystr = [str];

for (var i = 0; i < arraystr.length; i++) {
    if(str[i] === 's'){
       
    }
}
    TODO

